<nav class="pagination" role="navigation">
    {{#if prev}}
        <a rel="next" class="btn" href="{{page_url prev}}">
            <i class="fa fa-arrow-left"><</i>
            上一页
        </a>
    {{/if}}
    <div class="index">
        <span id="pageNum">{{page}}</span> of {{pages}}
    </div>
    {{#if next}}
        <a rel="prev" class="btn" href="{{page_url next}}">下一页
            <i class="fa fa-arrow-right">></i></a>
    {{/if}}
</nav>
